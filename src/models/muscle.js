class Muscle {
  constructor(details) {
    this.name = details.name; //Shoulder
    this.exercises = details.exercises; // Dips,Shoulder press,lateral raise
    this.imagePath = details.imagePath;
    this.parts = details.parts; // Front,Middle,Back
  }
}
