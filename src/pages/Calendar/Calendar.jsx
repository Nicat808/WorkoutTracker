import PageTitle from "../../components/Global/PageTitle/PageTitle";

const Calendar = ({ name }) => {
  return (
    <div>
      <PageTitle name={name} />
    </div>
  );
};

export default Calendar;
